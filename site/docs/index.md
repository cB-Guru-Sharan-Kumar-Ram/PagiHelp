---
layout: home
title: Pagination that compiles to SQL
titleTemplate: Pagination query builder for MySQL & PostgreSQL

# No `hero`/`features` frontmatter: the default home hero/features render
# nothing, and the bespoke "Query Console" landing is injected via the
# theme's `home-hero-before` slot (see .vitepress/theme/index.js).
---
